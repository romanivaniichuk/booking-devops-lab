apiVersion: v1
kind: Service
metadata:
  name: booking-app
  labels:
    app.kubernetes.io/name: booking-app

spec:
  selector:
    app.kubernetes.io/name: booking-app

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

  type: ClusterIP
